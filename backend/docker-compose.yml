services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - taxi-data:/srv/data
    environment:
      DATABASE_URL: sqlite:////srv/data/taxi.db

volumes:
  taxi-data:
