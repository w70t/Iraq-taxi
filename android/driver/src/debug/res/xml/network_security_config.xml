<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG ONLY: allows plain HTTP so the app can reach a local dev backend
     (e.g. http://10.0.2.2:8000). Release builds keep HTTPS-only. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
